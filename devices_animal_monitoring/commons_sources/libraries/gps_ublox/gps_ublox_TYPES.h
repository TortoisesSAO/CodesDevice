#ifndef GPS_UBLOX_TYPES_H
#define GPS_UBLOX_TYPES_H

/*
struct gps_settings
{
    volatile uint16_t uart_gps_bytes_read;//bytes readed in the last uart_read callback.
    char   uart_gps_rx_buffer[UART0_GPS_BUFFER_MAX];
    volatile uint16_t gps_bytes_acum;
    char gps_date_text[8];
};
struct gps_bufer
{char   uart_gps_rx_buffer[UART0_GPS_BUFFER_MAX];
    char gps_buffer_acum[UART0_GPS_BUFFER_MAX];
    char gps_last_message[_GPS_MESSAGE_FIELD_SIZE]; //D0 NOT CHANGE SIZE, MUST BE _GPS_MESSAGE_FIELD_SIZE
};
*/
#endif
